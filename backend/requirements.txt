# Silent Honor Foundation Backend Dependencies
# FastAPI & ASGI
# fastapi>=0.115 pulls in starlette>=0.40, fixing CVE-2024-47874 (unbounded
# multipart form-field buffering / DoS) -- relevant since this app accepts
# member signup + DD-214 uploads as multipart forms.
fastapi==0.115.3
uvicorn==0.24.0
# 0.0.20 still left several Dependabot-flagged issues open (parameter
# smuggling, unbounded preamble/part-header buffering, negative
# Content-Length memory exhaustion); 0.0.32 is current and clears all of them.
python-multipart==0.0.32

# MongoDB
motor==3.3.2
# 4.6.1 has an out-of-bounds read in the bson module (CVE-2024-5629).
pymongo==4.6.3

# Auth & Security
bcrypt==4.1.2
# 2.8.0 had several: JWK-as-HMAC-secret forgery, unknown `crit` header
# acceptance, PyJWKClient SSRF via file://, and an unbounded-decoding DoS.
PyJWT==2.13.0

# Validation & Config
pydantic==2.5.2
# Required by pydantic EmailStr fields (utils/validators.py, auth/contact models).
# Without it the app crashes on startup.
email-validator==2.1.1
# 1.0.0 has a symlink-following arbitrary file overwrite in set_key()
# (CVE-2026-28684). Not called anywhere in this app's own code today, but
# it's still a transitive load at import time.
python-dotenv==1.2.2

# File handling
aiofiles==23.2.1

# Email (Resend)
resend==0.7.0

# Supabase Storage (legacy fallback)
supabase==2.3.0

# AWS SDK (S3 storage + SES email)
boto3==1.34.69

# HTTP Client (version compatible with supabase)
httpx>=0.24.0,<0.25.0

# File encryption (DD-214 local fallback AES-256)
# 44.0.2 still bundled a vulnerable OpenSSL (GHSA, no CVE) and lacked SECT-curve
# subgroup validation (CVE-2026-26007); 48.0.1 is current and clears both.
cryptography==48.0.1
