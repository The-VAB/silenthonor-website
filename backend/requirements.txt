# Silent Honor Foundation Backend Dependencies
# FastAPI & ASGI
# fastapi>=0.115 pulls in starlette>=0.40, fixing CVE-2024-47874 (unbounded
# multipart form-field buffering / DoS) -- relevant since this app accepts
# member signup + DD-214 uploads as multipart forms.
fastapi==0.115.3
uvicorn==0.24.0
# 0.0.7+ fixes CVE-2024-24762 (ReDoS via a crafted Content-Type header).
python-multipart==0.0.20

# MongoDB
motor==3.3.2
pymongo==4.6.1

# Auth & Security
bcrypt==4.1.2
PyJWT==2.8.0
google-auth==2.36.0

# Validation & Config
pydantic==2.5.2
# Required by pydantic EmailStr fields (utils/validators.py, auth/contact models).
# Without it the app crashes on startup.
email-validator==2.1.1
python-dotenv==1.0.0

# File handling
aiofiles==23.2.1

# Email (Resend)
resend==0.7.0

# Supabase Storage (legacy fallback)
supabase==2.3.0

# AWS SDK (S3 storage + SES email)
boto3==1.34.69

# HTTP Client (version compatible with supabase)
httpx>=0.24.0,<0.25.0

# File encryption (DD-214 local fallback AES-256)
# 42.0.5 is ~2 years stale; bumped for the accumulated OpenSSL/CVE fixes since.
cryptography==44.0.2
