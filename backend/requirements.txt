# Silent Honor Foundation Backend Dependencies
#
# All versions below bumped to current stable (checked against live PyPI) to
# close out the Dependabot alerts on this manifest. `supabase` and `resend`
# were removed entirely -- storage.py's Supabase fallback and email.py's
# Resend integration both talk to their REST APIs directly over httpx and
# never import either SDK, so both packages (and Supabase's large transitive
# tree: postgrest/gotrue/storage3/realtime) were dead weight.
#
# FastAPI & ASGI
fastapi==0.140.0
uvicorn==0.51.0
python-multipart==0.0.32

# MongoDB
motor==3.7.1
pymongo==4.17.0

# Auth & Security
bcrypt==5.0.0
PyJWT==2.13.0

# Validation & Config
pydantic==2.13.4
# Required by pydantic EmailStr fields (utils/validators.py, auth/contact models).
# Without it the app crashes on startup.
email-validator==2.3.0
python-dotenv==1.2.2

# File handling
aiofiles==25.1.0

# AWS SDK (S3 storage + SES email)
boto3==1.43.56

# HTTP Client
httpx==0.28.1

# File encryption (DD-214 local fallback AES-256)
cryptography==49.0.0
